import { AddItemDirective } from './add-item.directive';

describe('AddItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AddItemDirective();
    expect(directive).toBeTruthy();
  });
});
